/**
 * Design System Migration Shims
 *
 * This file documents the planned re-exports from @yosemite-crew/design-system.
 * Once the package is installed, uncomment the exports below and existing imports
 * from @/app/ui will resolve to design system components.
 *
 * Install: pnpm add @yosemite-crew/design-system
 * Repo:    https://github.com/eng-AhmedMahmoud/design-system
 *
 * Usage after install:
 *   import { Button, Badge, Dialog } from '@yosemite-crew/design-system';
 *
 * Or via this shim:
 *   import { Button, Badge } from '@/app/ui/ds-shims';
 */

// Uncomment after installing @yosemite-crew/design-system:
//
// export {
//   Button, buttonVariants, Text, Card, cardVariants,
//   Badge, badgeVariants, Input, inputVariants, FormField,
//   Stack, Divider, Loader, Skeleton, Avatar, Toggle,
//   Accordion, SearchInput, FileUpload, EmptyState, SegmentedControl,
//   Dialog, DialogHeader, DialogBody, DialogFooter,
//   ToastProvider, useToast,
//   Tabs, TabList, TabTrigger, TabPanel,
//   Tooltip, Table, TableHeader, TableBody, TableRow, TableHead, TableCell, TableEmpty,
//   Select, Pagination, Breadcrumb,
//   colors, semanticColors, fontFamily, fontWeight, spacing, radii, shadows, cn,
//   useReturnFocus, useEscapeKey, generateId, srOnlyClass,
//   generateMeta, jsonLd, breadcrumbJsonLd,
//   escapeHtml, sanitizeUrl, generateNonce, buildCSP, recommendedSecurityHeaders,
//   useDebounce, useThrottle, useIntersection, usePreload, usePrefetch,
// } from '@yosemite-crew/design-system';

export {};
