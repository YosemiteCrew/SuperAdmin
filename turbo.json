{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "NODE_ENV",
    "NEXT_PUBLIC_APP_ORIGIN",
    "SUPERTOKENS_CONNECTION_URI",
    "SUPERTOKENS_API_KEY",
    "SUPERADMIN_BOOTSTRAP_EMAILS",
    "ADMIN_EMAILS",
    "PORT",
    "SONAR_TOKEN",
    "DATABASE_URL",
    "PANEL_BASIC_AUTH_CREDENTIALS",
    "AP_SIGNING_KEY",
    "AP_SIGNING_KEY_ID"
  ],
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "dependsOn": ["^build"],
      "outputs": [
        "build/**",
        ".vercel/**",
        "dist/**",
        ".next/**",
        "!.next/cache/**",
        "storybook-static/**"
      ]
    },
    "test": {
      "dependsOn": ["^build"],
      "passThroughEnv": ["DEVELOPER_DIR"],
      "cache": false
    },
    "test:ci": {
      "dependsOn": ["^build"],
      "passThroughEnv": ["DEVELOPER_DIR"],
      "cache": false
    },
    "lint": {
      "dependsOn": ["^build", "^lint"]
    },
    "type-check": {
      "dependsOn": ["^build", "^type-check"]
    },
    "dev": {
      "dependsOn": ["^build"],
      "cache": false,
      "persistent": true
    },
    "start": {
      "dependsOn": ["^build"],
      "cache": false
    }
  }
}
